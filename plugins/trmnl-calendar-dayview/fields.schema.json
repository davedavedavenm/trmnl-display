{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "title": "Calendar Week Ahead",
  "description": "7-day week agenda view showing events from Nango-connected calendars. Each calendar gets its own colour accent. Only days with events are shown.",
  "properties": {
    "nango_base_url": {
      "type": "string",
      "title": "Nango Base URL",
      "description": "Nango API base URL for calendar proxy",
      "default": "https://nango.example.com"
    },
    "nango_secret_key": {
      "type": "string",
      "title": "Nango Secret Key",
      "description": "Nango API secret key",
      "ui:widget": "password"
    }
  },
  "required": ["nango_secret_key"]
}
