# Full Repo + Live Audit — 2026-07-22

Scope: read-only audit of every tracked file in the repo plus live state on
`khpi5`, `trmnl-pi`, and `home-assistant`. No files were modified during the
audit.

---

## Live Infrastructure Status

| Host | Service | Status | Detail |
|---|---|---|---|
| **khpi5** | LaraPaper (`larapaper-app-1`) | Healthy | Up 27 h, image built 2026-07-14, 22 plugins |
| **khpi5** | Mode bridge (systemd) | Healthy | Active, `/health` returns `{"ok":true}` |
| **khpi5** | Cron (7 TRMNL jobs) | Healthy | ha-dashboard, bus, calendar, morning-mashup, jen-coming-home, sonos, db-restore |
| **trmnl-pi** | `trmnl-display.service` | Healthy | Active, `800x480 8-bpp → 4-bpp`, EP73_SPECTRA, 7200 s cycle, disk 18 % |
| **trmnl-pi** | Night-stop / morning-start timers | Healthy | Next stop 23:00, next start 06:40 |
| **home-assistant** | HA Core | Healthy | 2026.7.3 (latest), 5 TRMNL packages loaded |

### Live Display Verified

The last rendered PNG was pulled from the Pi and inspected visually:

- **Mode**: `calendar` (playlist 17, the only active playlist at audit time)
- **Plugin**: Calendar Day View (sidecar, plugin ID 27)
- **Content**: 3 events colour-coded by source (green = FAMILY_JTY, red = CB Holidays)
- **Output**: Full ACeP 6-colour indexed PNG, 800×480

Pre-rendered sidecar images for other modes were also verified on khpi5:

- Morning Mashup: commute HUD (42 min gauge, A4 route, light traffic) + HP quote
- Calendar Day View: colour-coded day layout
- Jen Coming Home: route panel
- Bus Departures: stop timetable
- Sonos Local: now-playing card

All render correctly with full colour.

### Active Playlist Roster

| Playlist | Mode | Active Plugin(s) | Refresh |
|---|---|---|---|
| 17 **(live)** | calendar | Calendar Day View | 7200 s |
| 11 | ha_dashboard | Home Assistant | 600 s |
| 14 | jen_morning | Jen Morning | 600 s |
| 15 | sonos | Sonos Local | 60 s |
| 16 | jen_commute | Jen Coming Home | 420 s |
| 18 | bus | UK Bus Departures | 600 s |
| 19 | idle | Calendar Day View | 600 s |

---

## Live Drift Found

| # | Issue | Severity | Detail |
|---|---|---|---|
| D1 | Mode bridge live ≠ repo | **HIGH** | Live `/home/dave/bin/trmnl-mode-bridge.py` (479 lines) has 2 endpoints missing from repo `scripts/trmnl-mode-bridge.py` (360 lines): `/health` and `/calendar/upcoming`. Also has calendar watcher thread, fire calendar integration, sonos cooldown, structured logging — none in repo. |
| D2 | Duplicate bridge scripts in repo | **MEDIUM** | Both `scripts/trmnl_mode_bridge.py` (156 lines) and `scripts/trmnl-mode-bridge.py` (360 lines) tracked. Neither matches live. |
| D3 | `trmnl-set-display-mode` not symlinked | **LOW** | Live `/home/dave/bin/trmnl-set-display-mode` is a standalone copy (267 lines, matches repo line count). Other scripts are symlinked from `trmnl-display-scripts/`. |

---

## CRITICAL Findings (P0)

| # | Area | Issue | Location |
|---|---|---|---|
| C1 | plugins/trmnl-calendar-dayview | 7 personal email addresses in `settings.yml` select options, repeated 12× (120 occurrences) | `settings.yml:43-50` ×12 |
| C2 | plugins/trmnl-calendar-dayview | 4 Nango connection UUIDs in select options, repeated 12× | `settings.yml:46-50` ×12 |
| C3 | plugins/trmnl-calendar-dayview | Private domain `nango.example.com` as default URL | `settings.yml:28`, `fields.schema.json:32` |
| C4 | plugins/trmnl-calendar-dayview | `payload.example.json` missing `merge_variables` wrapper | `payload.example.json:1` |
| C5 | plugins/trmnl-calendar-dayview | Personal medical data in payload example | `payload.example.json:15-20` |
| C6 | scripts/ | PII: calendar IDs, connection names, personal labels, workplace GPS | `nango_calendar_fetch.py:16-19`, `render_calendar_dayview.py:36-47`, `render_jen_coming_home.py:42` |
| C7 | scripts/ | Default Sonos entities expose room topology | `trmnl_sonos_local.py:32` |

**Phase**: P0 items are deferred to Phase 2 (snapshot-validated PII removal).
They are documented here for tracking but not actioned in Phase 1.

---

## HIGH Findings (P1) — Phase 1

| # | Area | Issue |
|---|---|---|
| H1 | config/packages/ | 10 REST commands hardcode `192.168.1.143` / `192.168.1.89` — use `!secret` |
| H2 | config/packages/ | 4 LaraPaper plugin UUIDs in REST command URLs |
| H3 | config/packages/ | `trmnl_calendar_refresh_sidecar` missing `Authorization` header |
| H4 | scripts/ | Hardcoded DB plugin IDs: 11, 22, 24, 27 — fragile |
| H5 | scripts/ | `run_trmnl_sonos.sh` 100 % hardcoded, zero env override |
| H6 | scripts/ | `trmnl_health_check.py:71`, `trmnl-display-shell.sh:81` hardcode URLs |
| H7 | Live drift D1 | Mode bridge repo copy missing live endpoints |

---

## MEDIUM Findings (P2) — Phase 1

| # | Area | Issue |
|---|---|---|
| M1 | plugins/ | 3 plugins missing `payload.example.json`: trmnl-alert, trmnl-multi-calendar, trmnl-sonos-local |
| M2 | plugins/ | 4 plugins missing `fields.schema.json`: trmnl-alert, trmnl-jen-coming-home, trmnl-multi-calendar, trmnl-sonos-local |
| M3 | plugins/ | 3 plugins missing Liquid template with no documented exception |
| M4 | plugins/trmnl-jen-morning | README `layout_variant` default and options are wrong vs `settings.yml` |
| M5 | plugins/trmnl-jen-morning | Liquid templates reference `theme` setting not in `settings.yml` |
| M6 | config/ | Real MAC address in `config/trmnl/config.example.json` |
| M7 | .gitignore | Missing `secrets.yaml`, `config/trmnl/config.json`, `.env.*` patterns |
| M8 | deploy/ | `docker-compose.yml` hardcodes `APP_URL` |
| M9 | deploy/ | `trmnl-crontab.txt` has unrelated homelab entries |
| M10 | scripts/ | `test_plugin.py` dead code |
| M11 | scripts/ | 3 shell scripts hardcode device `id=1` |
| M12 | scripts/ | Duplicate `trmnl_mode_bridge.py` (superseded) |

---

## LOW / INFO

- 3 plugins embed personal name "Jen" in directory name (functionally generic)
- `fields.schema.json` uses 3 different formats across plugins
- `COLOUR_SIDECAR_PATH.md` six- vs seven-colour wording inconsistency
- `TRMNL_PROGRESS_REPORT.md` / `TRMNL_PROJECT_PLAN.md` stale palette info
- `TRMNL_INTEGRATION_PLAN.md` pre-LaraPaper, superseded
- `deploy/systemd/` hardcoded `User=dave` and paths (acceptable for deploy reference)

---

## Secrets Scan: CLEAN

No live secrets committed. All tokens use env vars or `!secret`. Root `.env`
with live HA JWT is properly gitignored.

---

## Plugin Contract Scorecard

| Plugin | settings | README | Liquid | payload.ex | schema | Verdict |
|---|:---:|:---:|:---:|:---:|:---:|---|
| trmnl-ha-dashboard | Y | Y | Y | Y | Y | Gold standard |
| trmnl-jen-commute | Y | Y | Y | Y | Y | Complete |
| trmnl-hp-quotes | Y | Y | Y* | Y | Y | Complete |
| trmnl-jen-morning | Y | **drift** | Y | Y | Y | Fix README |
| trmnl-bus-departures | Y | Y | **gap** | Y | Y | Doc exception |
| trmnl-calendar-dayview | **PII** | Y | **gap** | **wrap** | **PII** | Phase 2 |
| trmnl-jen-coming-home | Y | **PII** | **gap** | Y | **gap** | Phase 1+2 |
| trmnl-alert | Y | Y | Y | **gap** | **gap** | Phase 1 |
| trmnl-multi-calendar | Y | Y | Y | **gap** | **gap** | Phase 1 |
| trmnl-sonos-local | Y | Y | Y | **gap** | **gap** | Phase 1 |

\* documented sidecar-only exception

---

## Remediation Phases

### Phase 1 — Zero-risk repo hygiene + security hardening

No live behaviour changes. Every edit is additive (new files) or
behaviour-preserving (gitignore, !secret, docs). Validated by
`py_compile`, `git diff --check`, secret scan, and visual sidecar comparison.

### Phase 2 — Snapshot-validated PII removal

Requires DB snapshot before/after each plugin edit. Deploy one file at a
time, verify Web UI shows current values, verify next sidecar render matches
pre-change PNG pixel-for-pixel. Rollback path: re-deploy old file, restart
service.

---

## Updates Available

| Component | Current | Action |
|---|---|---|
| LaraPaper | Image from 2026-07-14 (8 d) | Check `docker pull` on next maintenance window |
| Home Assistant | 2026.7.3 (latest) | None |
| Pi / show_img.bin | Working | None |
