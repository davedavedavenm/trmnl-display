# TRMNL-stack cron entries for khpi5. This is the TRMNL-relevant subset of the
# host crontab; unrelated homelab jobs (monitoring, fleet pipelines, etc.) live
# in the live host crontab but are intentionally not tracked in this repo.
PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
0 4 * * * /home/dave/bin/larapaper-upgrade-and-patch.sh 2>&1 | logger -t larapaper-update
# * * * * * /home/dave/run_trmnl_sonos.sh 2>&1 | logger -t trmnl-sonos-local
*/10 * * * * /home/dave/bin/trmnl-refresh-ha-sidecar 2>&1 | logger -t trmnl-ha-dashboard
0 5 * * * sudo python3 /home/dave/restore_color_model.py 2>&1 | logger -t trmnl-db-restore
*/10 * * * * /home/dave/bin/trmnl-refresh-bus-sidecar 2>&1 | logger -t trmnl-bus-departures
*/15 * * * * /home/dave/bin/trmnl-refresh-calendar-sidecar 2>&1 | logger -t trmnl-calendar-sidecar
*/5 * * * * /home/dave/bin/trmnl-refresh-morning-mashup 2>&1 | logger -t trmnl-morning-mashup
*/2 * * * * /home/dave/bin/trmnl-refresh-jen-coming-home-sidecar 2>&1 | logger -t trmnl-jen-coming-home
* * * * * /home/dave/bin/trmnl-reconcile-device-screen >/dev/null 2>&1
